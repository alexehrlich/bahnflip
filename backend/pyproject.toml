[project]
name = "bahnflip"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "requests",
    "python-dotenv",
]
[tool.setuptools.packages.find]
where = ["src"]
